# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SEHandler' do
  use_frameworks!

    pod 'Tagged', '~> 0.2'

  target 'SEHandlerTests' do
    inherit! :search_paths
    pod 'Nimble'
  end

end
